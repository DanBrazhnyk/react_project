import * as React from "react";
import { running } from "../importsImg/ImportsImg";
const Running = () => {
  return (
    <img
      src={running}
      alt="error"
      style={{
        height: "118px",
        width: "210px",
        marginLeft: "8px",
        marginTop: "25px",
      }}
    />
  );
};
export default Running;
